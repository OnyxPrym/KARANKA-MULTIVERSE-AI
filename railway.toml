[build]
builder = "nixpacks"

[build.args]
PYTHON_VERSION = "3.9"  # Changed from 3.11 to 3.9

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"

[variables]
DERIV_APP_ID = "1089"
